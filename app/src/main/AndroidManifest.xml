<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nivacreation.login">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/bus_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/bus_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Login">
        <activity android:name=".Driver_Navigation"></activity>
        <activity android:name=".Passenger_Navigation" />
        <activity android:name=".Bus2_Details" />
        <activity android:name=".BusDetails" />
        <activity android:name=".Welcome" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".Admin" />
        <activity android:name=".Driver" />
        <activity android:name=".qrScanner" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".layout_header" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".splashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/> <!-- optional (needed if default theme has no action bar) -->

    </application>

</manifest>